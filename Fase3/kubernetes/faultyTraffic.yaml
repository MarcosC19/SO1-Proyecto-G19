apiVersion: split.smi-spec.io/v1alpha1
kind: TrafficSplit
metadata:
  name: faulty-traffic
  namespace: squidgame
spec:
  service: svc-ruta-rabbit
  backends:
  - service: svc-ruta-rabbit
    weight: 500m
  - service: svc-ruta-kafka
    weight: 500m